import converted
from converted import kg_to_lbs
from converted import lbs_to_kg
from utils import find_max
print(converted.kg_to_lbs(25))
print(kg_to_lbs(25))

numbers = [1,2,5,10]
print(f"Max of array = {find_max(numbers= numbers)}")
# ham max nay co san trong python
print(f"Max = {max(numbers)}")